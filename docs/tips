问题解决了，环形连接器使用没问题。
不过有另外一个问题，当我想使用启动链模式时候，想添加引擎，但是引擎被遮罩了，我想是否可以通过透视将引擎显示出来。
这里有一个逻辑，必须先将环形连接器断开才能启动引擎。
根据这个逻辑，我们可以在将环形连接器被设置为启动链的目标之一时候，就打开透明，此时环形连接器也无法成为新的目标（因为它已经作为之前的确定任务的目标了），这样自然地点透视后的被遮罩的引擎时就将引擎作为目标加入启动链的任务中。
合并。
